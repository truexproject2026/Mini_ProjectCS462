=====================================================
คู่มือการติดตั้งระบบออนไลน์ (Vercel + Render + Cloudinary)
=====================================================

โปรเจคนี้ใช้โครงสร้างแบบ Hybrid Cloud เพื่อรองรับ Machine Learning (V8.1):
1. Frontend (Next.js) -> ฝากที่ Vercel (https://mini-pj-online.vercel.app/)
2. Backend (FastAPI) -> ฝากที่ Render.com (https://mini-projectcs462.onrender.com)
3. Dataset Storage   -> ฝากที่ Cloudinary (เก็บรูปภาพถาวร)

-----------------------------------------------------
ขั้นตอนที่ 1: ตั้งค่า Cloudinary (สำหรับเก็บรูป Dataset)
-----------------------------------------------------
1. สมัครสมาชิกที่ https://cloudinary.com/
2. ในหน้า Dashboard ก๊อปปี้ค่าดังนี้:
   - Cloud Name
   - API Key
   - API Secret (กดปุ่มรูปดวงตาเพื่อดู)
3. สร้าง Folder ชื่อ ๓๖, ๓๗, ๓๘, ๓๙, ๔๐ ไว้ใน Cloudinary (ถ้าต้องการจัดระเบียบ)

-----------------------------------------------------
ขั้นตอนที่ 2: ตั้งค่า Backend ไปที่ Render.com
-----------------------------------------------------
1. สมัครสมาชิก Render.com และเชื่อมต่อ GitHub
2. กด "New" > "Web Service" เลือก Repository ของคุณ
3. ตั้งค่าดังนี้:
   - Name: thai-handwriting-api
   - Runtime: Python 3
   - Root Directory: thai-handwriting-next
   - Build Command: pip install -r backend/requirements.txt
   - Start Command: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
4. กด Deploy และก๊อปปี้ URL ของ Backend มา (เช่น https://...onrender.com)
*หมายเหตุ: Render Free Tier จะมีการ Cold Start (รอประมาณ 30-60 วินาทีในการเรียกใช้ครั้งแรก)*

-----------------------------------------------------
ขั้นตอนที่ 3: ตั้งค่า Frontend ไปที่ Vercel
-----------------------------------------------------
1. เข้า Vercel และเลือก "Add New" > "Project" ดึงมาจาก GitHub
2. ในหน้าตั้งค่า ให้ใส่ Environment Variables ดังนี้:
   - BACKEND_URL = (URL จาก Render ในข้อ 4)
   - NEXT_PUBLIC_BACKEND_URL = (URL จาก Render เหมือนข้างบน)
   - CLOUDINARY_CLOUD_NAME = (ค่าจากขั้นตอนที่ 1)
   - CLOUDINARY_API_KEY = (ค่าจากขั้นตอนที่ 1)
   - CLOUDINARY_API_SECRET = (ค่าจากขั้นตอนที่ 1)
3. ตั้งค่า Output Directory เป็น: .next (ใน Settings > General)
4. กด Deploy

-----------------------------------------------------
ขั้นตอนที่ 4: การอัปเดตโมเดลแบบ Real-time (V8.1)
-----------------------------------------------------
- เข้าหน้าเว็บไปที่ส่วนของ Admin
- คุณสามารถอัปโหลดโมเดลใหม่ (`.pkl`) และไฟล์ Metrics (`.json`) ได้ทันที
- ระบบจะส่งไฟล์ตรงจาก Browser ไปที่ Render Backend และโหลดเข้า Memory ทันที
- **ข้อควรระวัง:** การอัปโหลดผ่านหน้าเว็บเป็นการเปลี่ยนค่าในหน่วยความจำ (Temporary) หาก Render รีสตาร์ทจะกลับไปใช้ไฟล์ใน GitHub ดังนั้นหากต้องการเปลี่ยนถาวรต้อง Commit ไฟล์ลง GitHub ด้วย

=====================================================
โปรเจควิชา CS462 Data Analytics and Mining
สถานะปัจจุบัน: V8.1 ออนไลน์สมบูรณ์ (Predict + Collect + Real-time Update)
ความแม่นยำล่าสุด: 96.36% (ExtraTrees Model)
=====================================================
